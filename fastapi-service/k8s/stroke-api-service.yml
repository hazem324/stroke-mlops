apiVersion: v1
kind: Service
metadata:
  name: stroke-api-service
  namespace: stroke-ml

spec:
  type: NodePort

  selector:
    app: stroke-api

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080